

services:
  grc_api:
    build: 
      dockerfile: Dockerfile
      context: ./
    ports:
      - "80:80"
    env_file: ./.env
    volumes:
      - .:/src

networks:
  default:
    name: kafka-network
    external: true
